# Market Digest — 每日股市字幕分析

用 yt-dlp 抓取 YouTube 財經頻道最新影片字幕，分析市場重點，輸出 Markdown 日報。

## 觸發方式

使用者會說「看有沒有最新一集」或類似指令。流程：

1. 執行 `bash scripts/fetch.sh` 抓取所有頻道最新影片字幕
2. 讀取 `subtitles/` 資料夾中的 .vtt / .srt 檔案
3. 對每個檔案進行摘要與分析
4. 產出一份 Markdown 日報存到 `reports/YYYY-MM-DD.md`

## 頻道清單

定義在 `scripts/channels.txt`，格式為每行一個 YouTube handle 或頻道 ID，# 開頭為註解。
新增或移除頻道只改這個檔案。

## 指令

- `bash scripts/fetch.sh` — 抓取所有頻道最近一支影片的字幕，存到 `subtitles/`
- `bash scripts/fetch.sh @kukantieh` — 只抓特定頻道

字幕檔命名規則：`{handle}_{videoId}_{lang}.vtt`

## 分析規則

對每支影片的字幕，產出以下結構：

```
### {頻道名稱}：{影片標題}
- **發布時間**：YYYY-MM-DD
- **摘要**（3-5 個重點，每點一句話）
- **提及的標的**：列出提到的股票、ETF、指數、商品
- **市場情緒判斷**：偏多 / 偏空 / 中性，附一句話理由
```

## 日報格式

```markdown
# 市場日報 YYYY-MM-DD

## 總覽
（綜合所有頻道觀點，2-3 句話描述今日市場共識與分歧）

## 多空方向
- 偏多訊號：...
- 偏空訊號：...
- 觀望因素：...

## 各頻道摘要
（按上方結構逐一列出）

## 跨頻道交叉比對
（不同頻道對同一事件是否有不同解讀，列出分歧點）
```

日報存到 `reports/YYYY-MM-DD.md`。

## 注意事項

- 字幕是語音辨識產生的，有錯字很正常，不要逐字引用，理解語意即可
- 直播類影片（游庭皓）字幕品質較差，分析時容忍度要高
- 如果某頻道今天沒有新影片，在日報中註明「今日無更新」
- 不要對投資方向給出建議，只做事實摘要與情緒判斷
- 中文頻道用中文摘要，英文頻道用中文摘要（統一語言）
